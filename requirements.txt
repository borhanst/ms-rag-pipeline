langchain>=0.2.0
langchain-google-genai>=1.0.0 # Ensure you have a recent version
langchain-community>=0.2.0
pypdf
python-dotenv
tenacity
faiss-cpu # Or faiss-gpu
# Pin the google-generativeai version explicitly
google-generativeai>=0.5.0 # Or check for the latest stable version
# Potentially update protobuf if needed (sometimes downgrading works)
# protobuf>=4.21.0 # Try different versions if the error persists